(module main (block
  #! Hello World example

  (import std.io)
  (import std.string _ removeAll string)

  (sig main (Fun IO IO))
  (val main (fun io (println io (string.removeAll '\'' "Hel'lo World!"))))
))
